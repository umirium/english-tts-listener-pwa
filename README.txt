English TTS Listener

構成:
- index.html: UI
- storage.js: localStorage管理
- speech.js: speechSynthesis制御
- app.js: UIロジック
- sw.js: PWAキャッシュ
- manifest.json: PWA設定

GitHub Pages に置く場合は、すべて同じ階層に配置してください。
iPhoneでは Safari で開き、ホーム画面に追加すると使いやすいです。
